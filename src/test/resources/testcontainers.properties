# Forzamos el uso del pipe de Windows directamente
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
# Deshabilitamos Ryuk (el contenedor que limpia), a veces Windows lo bloquea
testcontainers.reuse.enable=true
checks.disable=true